# update_listener
Updates the listener for all sounds.

`void update_listener(vector cd, double rotation);`

## Arguments:
- `vector cd`: The listener coordinates in vector form.
- `double rotation`: The listener's rotation.
