# nv_audio
A new and resigned audio and sound management system, main class.

`nv_audio(mixer@ mix = null, pack_interface@ pack = sound_default_pack);`

## Arguments:
- `mixer@ mix = null`: The mixer to set on this sound manager (optional).
- `pack_interface@ pack = sound_default_pack`: The pack to use (optional).
